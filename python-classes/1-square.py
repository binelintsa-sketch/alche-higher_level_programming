#!/usr/bin/python3
"""module that defines a Square class with a private size attribute."""

class Square:
    """class Square that defines a square but its size."""

    def  __init__(self, size):
	"""inititialize a new Square instance.

	Args:
	    size: The  size of  the  square.

	"""
	self.__size = size

